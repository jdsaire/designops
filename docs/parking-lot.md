# Parking Lot

One line each, no elaboration. Reviewed once, per the Execution Roadmap's Rule 1 — never before.

- Evaluate Web Components / single-file colocation for the per-organism structure once the multipage migration lands.
- Evaluate adopting CSS `@layer` for cascade management across base/shared/home partials.
- LimaFly app build cancelled; old Wave 3 will not resume in any form.
- Old Brief 04 (LimaFly App) archived — will not ship as an airport-app brief.
- Old Wave 1 Figma-export debt retired as a hard blocker; only the slice evidencing Brief 03 remains, as an optional principal task.
- /work/limafly-app route slot retired pending a new slug decision at Gate G1 (see Execution_Roadmap_v2_0.md §8).
- Execution_Roadmap_v2_0.md supersedes v1.0 as of this commit; v1.0 retained in docs/ as history, not deleted.

## Stale ES keys — Wave 6 Sonnet translation pass (never hand-write Spanish)

Wave-4 Brief 01 edits changed EN-only surfaces. The following ES counterparts are now STALE and must be retranslated:
- Brief 01 (`work-designops-system.html` BODY dict) E1 surfaces: `hero_deck`, `kw1`, `hero_rv_txt`, `act3_c1_x`, `act3_c3_x`.
- Brief 01 E4 surfaces: `hero_read`, `act3_c2_rv_btn`, `act3_c2_rv_txt`.
- Brief 01 E2 new Schedule block (whole namespace): `sch_eyebrow`, `sch_verdict`, `sch_lede`, `sch_e1_t`–`sch_e10_t`, `sch_e1_m`–`sch_e10_m`, `sch_lg1`, `sch_lg2`, `sch_lg3`, `sch_note`.
- Brief 01 renumbered act eyebrows (display numerals 03→04, 04→05, 05→06): `act3_eyebrow`, `act4_eyebrow`, `act5_eyebrow` — plus their ES counterparts once the ES chassis exists.
- Home (`assets/i18n/es.json`): `work_card1_body` (superseded by the EN hero short version).
- Exception — NOT stale: `work_card1_cta` ES = "Acceder al Brief" is principal-supplied and populated.
- ~~Wave-4.5 CHROME split new EN-only keys awaiting ES: `chrome_wordmark_sub`, `chrome_nav_cta`.~~ **Closed in PR-5**: both now populated in `es.json` (`chrome_wordmark_sub` kept identical as a brand label; `chrome_nav_cta` = "Iniciar conversación", reusing the existing `work_cta` pattern). This was the only genuinely-missing (not merely stale) CHROME key pair, and its absence meant every brief page's nav bar showed English regardless of the selected language.
- Wave-4.5 BODY split (`assets/i18n/briefs/designops.en.json`) — full Brief 01 BODY namespace is EN-only; `designops.es.json` is an empty stub pending the Wave 6 ES pass.
- Wave-5 PR-1 IA migration: Brief 01's BODY dict moved to `work/designops-system/i18n/` — no key changed, ES staleness unaffected.
- Wave-5 PR-2 Brief 04 page (`work/yape-trust-verify-brief/i18n/yape-trust-verify.es.json`) — the **entire 135-key BODY namespace is EN-only**, mirrored from EN as a temporary value so SwapLang renders rather than falling through. Every key awaits the Wave 5.6 Sonnet ES pass: `hero_*` (8), `act0_*` (10), `act1_*` (14), `act2_*` (10), `act3_verdict`/`act3_eyebrow`, `gantt_*` (10), `act4_*` (14), `act5_*`/`m1_l`–`m8_p` (24), `r1`–`r3`, `act6_*` (14), `evidence_*`, `convert_*`, `band_m_*`. Full key list is the JSON file itself — it and the EN file have identical key sets by construction.
- Wave-5 PR-3 placeholder pages — each ships a 135-key EN-only BODY namespace with ES mirrored from EN as a temporary value, awaiting the Wave 5.6 Sonnet pass: `work/tuua-transfer/i18n/tuua-transfer.es.json` and `work/limafly-ux/i18n/limafly-ux.es.json`. Only the hero keys (`hero_eyebrow`, `hero_status`, `hero_title`, `hero_deck`, `kw1`–`kw4`, `hero_rv_btn`, `hero_rv_txt`) plus the in-progress keys (`wip_tag`, `wip_txt`, `scaffold_tag`, `scaffold_txt`) are worth translating — the remaining keys are scaffold copy that gets deleted, not translated, when each brief's real copy lands.
- Wave-5 PR-4 Main upgrade (`assets/i18n/es.json`) — 20 new EN-only keys mirrored as temporary EN, awaiting the Wave 5.6 Sonnet pass: `work_card4_label`, `work_card4_heading`, `work_card4_body`, `filter_label`, `filter_clear`, `filter_cat_industry`, `filter_cat_field`, `filter_cat_role`, `filter_status`, `filter_empty`, and the 13 `tag_*` values less those already legible in both languages. **Not stale:** `work_card{2,3,4}_cta` — all three reuse `work_card1_cta`'s principal-supplied ES string "Acceder al Brief" verbatim, which is data reuse rather than hand-written Spanish. `work_lede` was updated in both languages (three → four builds) and is likewise not stale.
- `filter_status` carries `{shown}` / `{total}` placeholders that the ES translation must preserve; the module substitutes them at runtime.
- Wave-6 task 6.2.1 retired five EN-only keys whose surfaces it deleted — `filter_cat_status`, `tag_live`, `tag_wip` (Status filter removed), `footer_map_label`, `footer_sla` (footer site map reverted); 6.3's EN-only debt therefore drops from fifteen to **ten** remaining: `filter_open_cat`, `filter_remove_label`, and the eight `evo_m{3,4,6,7}_rv_{btn,txt}` (`filter_remove_label` survives as a retained orphan under PICK A1, which made the in-card tags read-only).
- Wave-6 task 6.2.1 orphaned-but-RETAINED (deliberate survivors, both dictionaries, not oversights): `nav_logo_alt` (logo became a text wordmark), `hero_cta` + `hero_cta_aria` (hero "See the work." CTA removed), `filter_remove_label` (mini-chip host removed); `filter_label` stays LIVE — moved onto the `.tagfilter` container as its `data-i18n-aria` accessible name.
- Exception — NOT stale (task 6.2.1): `nav_capabilities` ES = "Verticales" and `nav_evolution` EN = "About" are principal-supplied verbatim, mirroring the `work_card1_cta` exception above.
- **Wave-6 task 6.2.2 added ZERO ES debt.** The roadmap's EN-only-then-6.3 rule was revoked for this run by principal instruction: the 14 new keys were authored in EN and given ES the same pass via `/designops-copy-es`, in a dedicated commit — `home_cap_cta`, `home_about_head`, `home_about_lede`, `home_about_cta`, `home_contact_lede`, `about_hero_eyebrow`, `cap_hero_head`, `cap_hero_lede`, `about_hero_head`, `about_hero_lede`, `about_cv_head`, `about_cv_cta`, `contact_github`, `contact_elsewhere_head`. Existing ES values were preserved from their point of origin; no per-brief BODY dict touched. The **ten inherited EN-only debt keys still remain** for 6.3: `filter_open_cat`, `filter_remove_label`, and the eight `evo_m{3,4,6,7}_rv_{btn,txt}`. No keys were retired this run.

## Logged tensions (Wave 6 · Task 6.2.2 — multipage migration)

- **§JDIGITAL admit/reject verdict (so it is not re-argued).** From `jdsaire/jdigital`, all REJECTED: the hero counter block ("A decade across three disciplines" / "…shaped by the work" / "Projects shipped across 5 industries" / "Faster builds with AI") — unsourced agency numbers, not in the frozen inventory; the first-person-plural / practice voice ("Our non-negotiables", "JDigital works best where…") — solo authorship, no "we"; the tool-dependency disclosure (jdigital's line naming specific commercial AI subscriptions as a service guarantee) — vendor-guarantee framing + collides with the sole-authorship rule; the "9-in-1" role constellation — already discarded by the principal. The operating-principles triad (Zero Improvisation / Zero Noise / Zero Overpromising) was **considered and DROPPED** — two of three bodies are rejected material, and a rewritten triad would read as a vendor pitch on a personal About page; the timeline + CV are the stronger sourced content. **No jdigital content is inherited anywhere.**
- **[GAP] shipped: none. [TEMPORAL] shipped: none.** The CV is a real supplied file (`assets/docs/CV_Juan_Diego_Saire.pdf`) hosted on /about/, so its affordance links out for real. The contact form's Formspree `action="…/YOUR_FORM_ID"` travels verbatim as a pre-existing placeholder (not this run's to complete).
- **Authorized deviations:** (1) inserted ahead of 6.3 (roadmap-sanctioned); (2) ES generated this pass instead of deferred (principal override); (3) two commits instead of one — a dedicated ES commit (principal instruction); (4) Capabilities delivery reverted from vertical stacked sections back to the retained carousel across all breakpoints (principal, post-Gate-B) — supersedes success-criterion 6; (5) CV placed on /about/ not /contact/ (principal) — Contact carries LinkedIn + GitHub only, deviating from success-criterion 8.
- `assets/js/pages/home/nav.js` (Home scrollspy) **deleted** — its only surface, the IntersectionObserver over `#capabilities`/`#evolution`, is removed this run (both became page routes). Page-level current state is now an authored `aria-current="page"` on each dedicated page's own nav + overlay link, styled via a new `[aria-current="page"]` rule in `nav.css`.
- **CSS-map correction (structural, not a copy claim):** the base `.section`/`.section__inner`/`.section__eyebrow*` idiom lives in `pages/home/work.css` and `.cta`/`.cta__label`/`.cta__icon` in `pages/home/hero.css` — neither linked by the dedicated pages, yet the shared footer + page bodies depend on them. Both were reproduced VERBATIM into `assets/css/shared/page-hero.css` (the 6.2.1 nav-scoped-reproduction precedent). If either source changes, update the reproduction.
- The dedicated-page hero reproduces the brief hero's `min-height:100svh` verbatim (principal's "inherit the brief hero" rule) — a full-viewport hero precedes each page's body. Revisit if it reads as too tall on the content pages.
- About and Contact each carry two headers by design (page-hero eyebrow + the organism's own header on About; the page hero supersedes the form's `.contact-header` on Contact, which is not re-rendered). `contact_eyebrow`/`contact_headline`/`contact_lede` stay LIVE via the Contact page hero.
- `cap_hero_head`/`about_hero_head` use "operator/operador" positioning not otherwise established in ES — acceptable in Lima business register ("operador del aeropuerto"); revisit if a crisper ES term is preferred at 6.3.

## Logged tensions (Wave 5)

- Tag filter matching is **faceted**, not a strict global AND: a card must satisfy every category that has an active chip, and within a category any one chip suffices. The roadmap says multiple filters shortlist to "the intersection"; with a four-card pool a literal AND across every chip dead-ends on the second click (Banking + Airport would always return nothing). The intersection is therefore taken across categories, which is the standard faceted-search reading and keeps the pool navigable. Revisit if the card count grows enough to make strict AND usable.
- `assets/img/work/build-in-progress.svg` was renamed to `verify.svg` and is now Work Card 4's background. It was an orphan — referenced nowhere, and its filename described an in-progress treatment that PR-3 implemented in CSS instead. Card 4 needed art, it was the only unused asset in the set, and shipping a path named "build-in-progress" on a finished brief would have misread at source level. No capability lost; the in-progress treatment is the CSS one.
- Work cards are narrower at desktop going from three columns to four (about 319px wide against the previous 432px) — this width reduction is accepted and unchanged. **Height is no longer coupled to it as of PR-5**: card height had been `aspect-ratio`-driven, so it shrank by the same ~26% as width, which broke the hover overlay (title + summary + CTA no longer reliably fit). PR-5 gives `.work-card` an explicit `height: clamp(384px, 37.5vw, 540px)` at desktop (`work.css`, after the tablet breakpoint), matching the old 3-column height at every desktop width, and adds a `-webkit-line-clamp: 6` safety net to `.work-card__summary` as insurance. If a fifth card is ever added, the grid still needs a row break rather than a fifth column — width, not height, is the open concern now.

- `assets/img/work/build-in-progress.svg` was **not** used as the in-progress badge on the placeholder pages, despite the deployment naming it. Inspected: it is a 2.2 MB abstract purple gradient at 3279×4918, the same art family as `build.svg` / `control.svg` / `scale.svg` — a work-card background whose only in-progress semantics are in its filename. As a page badge it would communicate nothing while costing 2.2 MB per page. The pages instead carry a legible in-progress treatment built from existing tokens (dashed purple, uppercase, the established `.temporal` idiom). The asset remains available for a home work-card background if cards 2/3 should ever be marked in-progress there — not done, because it would make those two cards visually identical and their CTAs already lead to badged pages.
- Placeholder scaffold retains Brief 04's real measured figures (Act 05) and its staged-security material (Act 04). Both sit inside badged, dimmed scaffold acts on pages whose banner states the text belongs to another brief. Deleted outright when real copy lands; until then the badging is the only thing keeping those figures from reading as Brief 02/03 claims.

- Brief 04 Act 03 is a reconstructed effort chart, not a recorded sprint artifact: the v4 spec reserved this act for a future Gantt build, and the deployment authorized instantiating it. Story points express relative effort derived from the Wave 4 plan's phases and the completion report; the on-page caption states this plainly. If a real estimation record ever exists, replace the data object.
- Brief 04's `hero_read` now reads "7 min read", replacing the inherited-and-unverified "4 min read" that Copy v4's own fact register flagged and the spec's open item 7 authorized adjusting. Measured from the page: 1,517 surface words, ~6.3–7.6 min at 200–240 wpm; reveals add roughly two minutes more for a reader who opens them. Re-measure if the page's surface copy changes materially.
- Continuity card `../tuua-transfer/` 404s until the Wave 5 PR-3 placeholder pages land. Authored against the planned route, per the Copy v4 open item.
- **PR-5 root cause, for the record:** PR-1's file move left `assets/js/pages/home/nav.js` importing `../core/i18n.js` — correct from the pre-move location (`assets/js/home/`), broken from the post-move one, since it now needed `../../core/i18n.js`. A static ES-module import failure fails the whole graph, so `main.js`'s `DOMContentLoaded` handler silently never ran on Home — not just the language toggle, but the tag filter, carousel, evolution, contact, and ticker too. None of this was caught at the time because verification that PR (and every PR since) could confirm resource resolution and DOM-handle existence, but not execute JS — this specific bug only shows up at runtime. One-line fix in PR-5.
- **Expected, not a bug:** after PR-5, brief-page **body** copy (Acts, hero deck, cards) still renders in English when ES is selected, on all four brief pages. Brief 01's body ES dict is empty; Brief 04/placeholder body ES dicts are EN-mirrored — both already logged above, both awaiting the Wave 5.6 Sonnet pass. PR-5 only closed the CHROME-level gap (nav wordmark + CTA); the body-copy gap is unchanged and intentional.

## Logged tensions (Wave 4)

- Eyebrow vs. copy: `hero_read` now reads "5 min read" while the locked hero short version (`hero_rv_txt`, E1-verbatim) still says "the next four minutes." Reconcile in a later copy pass if desired.
- D8 framing: Act 04 dashboard `dash_m5` cell ("0" · "Frameworks / dependencies") is a measured fact, not the identity claim, and Act 04 is frozen. The D8 governance line is now "No framework I couldn't justify" — a softer stance than a literal "0." Revisit the numeric cell's framing when Act 04 next opens.
- Evaluate inheriting JDigital_NetworkingTour toggle file's animation/CSS system into work-brief components (post-freeze).
- Definitive SVG cover art for the four work cards, replacing the placeholder containers.
- Verify whether Coursera transfers course completion between the Front-End and Full-Stack certificates.
- Correct the stale Wave-5 ES parity entries: all four brief dictionaries are already translated.
- The Yape build ships a Vite/npm toolchain (package.json, vite.config.ts, devDependencies) inside a repository declared zero-dependency and buildless.
- The oversized assets/img SVGs carry embedded base64 JPEG rasters rather than vector geometry.
- docs/W7/1_CV_JDSaire_UltraSpec_v1_0.md is a v1.0 copy of a spec that has since advanced.
- Brief renumbering pending once all four briefs exist: the TUUA and LimaFly merge leaves a gap in the 01-05 sequence.
- AccreditaPass ux-ui/05-iteration/README.md line 5 calls ShopEase a course-mate's build; its own Gate 0 file names jdsaire/frontend_c6_ecommerce - correct the README line.
- S2's interview question set shows no issue date; freeze D11's 72-hour clock is unstarted.
- Home's work pagination dots are hardcoded at four and labelled "Card N of 4" while the section carries five cards.
- Build a JS/TS equivalent of AccreditaPass in a new repo, assessing WebSockets and other unlearned tooling, as post-application backup for hiring-manager inspection.
- Inherit the progressive-disclosure components from briefs 1 and 4 into the AccreditaPass brief and reallocate surface prose behind them.
- Build a Gantt component and a three-level KPI set for the AccreditaPass brief, matching the components briefs 1 and 4 already carry.
- Add a Next Steps section to the AccreditaPass brief narrating the planned JS/TS equivalent build.
- capabilities/ no longer carries per-vertical prose Main does not after the H1 restoration; a later stage should re-deepen it so the route stays justified.
- The nav Work submenu is stale on all four pages: it points at the retired designops-system, tuua-transfer and limafly-ux routes and carries no AccreditaPass entry.
- The 13 tag_* dictionary pairs in both Home dictionaries are orphaned by the tag-filter removal and are cleanup candidates at the closure audit.
- Redesign and restore the site-wide footer; the hide is a holding action, markup retained in source.
- Include Credly badges (Microsoft, Google, Claude, McKinsey) to populate the About child.
- Amendment F3 (signed 31-08-2026) executed six previously parked Home items and killed three; they are settled, not dropped.
